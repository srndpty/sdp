# AGENTS.md

sdp リポジトリで作業するエージェント・開発者向けの規約。
設計の詳細は [docs/architecture.md](./docs/architecture.md)、
開発計画は [docs/development-plan.md](./docs/development-plan.md) を参照する。

## 言語

- AI チャットの返答は日本語で行う。
- コードコメント、docstring、設計文書、README、ユーザー向けメッセージ、
  コミットメッセージなど、人間が読むことを想定した文章は日本語で統一する。
- ライブラリ名、API 名、識別子、一般的な技術用語は無理に翻訳しない
  （例: QMediaPlayer、リングバッファ、FFT）。
- コードの変数名、関数名、クラス名は英語とする。テスト関数名も英語とし、
  何を確認するテストかは docstring で日本語で説明する。

## 変更時の基本方針

- 変更前に周辺コード、既存テスト、設計文書を読む。
- 既存の設計、命名、テスト方針へ合わせる。
- ユーザーの未コミット変更を無断で戻さない。
- 依頼範囲外の変更を混ぜない。
- 一つの変更目的に対して、レビュー可能な小さな差分にする。
- 完了前に format、lint、型検査、テストを実行する
  （`pwsh -File scripts/fix.ps1` のあと `pwsh -File scripts/check.ps1`）。

## DRY と抽象化

- 同じ知識や業務規則を複数箇所に持たせない。
- 単なる数行の類似だけを理由に共通化しない。
- 将来必要になるかもしれない、という理由だけで抽象化を増やさない。
- 抽象化は責務境界または現実の重複が明確になってから導入する。
- 汎用プラグイン基盤は作らない。

## 責務の分離

- UI、再生制御、再生バックエンド、プレイリスト、音声解析、永続化、設定、
  Windows 統合を分離する。
- UI から QMediaPlayer などの具体的な再生実装を直接操作しない。
  UI が触ってよいのは PlaybackController までとする。
- MainWindow を god class にしない。レイアウト骨格とウィジェットの配線に留める。
- ファイル I/O や重い処理で GUI スレッドをブロックしない。
- 純粋ロジック（FFT、波形縮約、次曲決定など）は Qt オブジェクトから分離し、
  単体テスト可能にする。

## フォールバックと互換性

- 開発中は、未要求の後方互換レイヤー、互換用分岐、旧形式サポート、
  暗黙のフォールバックを原則として追加しない。
- エラーを隠して別実装へ切り替える silent fallback を作らない。
- フォールバックが必要な場合は、理由、発動条件、観測可能なログ、削除条件を明記する。
- Qt Multimedia から mpv への変更は runtime fallback ではなく、
  P0 結果に基づく ADR 上の明示的な設計変更として行う
  （[docs/adr/0001-playback-engine.md](./docs/adr/0001-playback-engine.md)）。
- 設定ファイルの migration も、実際の旧バージョンが生まれるまでは過剰に作り込まない。

## エラー処理

- 広すぎる例外捕捉で不具合を隠さない。
- 回復可能な失敗とプログラミングエラーを区別する。
- エラー時は原因をログへ残す。
- 再生失敗、メタデータ失敗、解析失敗を独立して扱う。
  一つの失敗が他機能を止めないようにする。
- テストを通すためだけの例外握り潰しをしない。

## 依存関係

- 標準ライブラリで十分な場合は依存を増やさない。
- 新しい依存を追加する際は、用途と既存手段では不足する理由を確認する。
- P0 では技術検証に必要な依存だけを追加する。
- lockfile を更新せず pyproject.toml だけを変更しない
  （`uv add` を使うか、変更後に `uv lock` を実行する）。

## Git

- コミットメッセージは日本語で記述する。
- コミットには一つの論理的変更だけを含める。
- ユーザーの明示依頼なしに push、force push、rebase、amend、履歴書き換えを行わない。
- 生成物、キャッシュ、個人設定、音声デバイス固有情報をコミットしない。
